# メモアプリSPA（React）

## 概要

このアプリは、フィヨルドブートキャンプの課題であるReactを使ったシングルページアプリケーション（SPA）のメモアプリです。
メモを作成・編集・削除できます。保存にはローカルストレージを使っています。

## セットアップ方法

- リポジトリをクローン
  `git clone` を行い
  `cd `でリポジトリに移動してください

- パッケージをインストール
  `npm install`でインストールしてください

- 開発サーバーを起動
  `npm run start`で起動します
